steps:
  - label: "🤡 :hammer:"
    plugins:
      - docker-compose#v4.1.1:
          run: tests
  - label: ":sparkles: lint"
    plugins:
      - plugin-linter#v3.0.0:
          id: artifacts
  - label: ":shell: Shellcheck"
    plugins:
      - shellcheck#v1.3.0:
          files: hooks/**
